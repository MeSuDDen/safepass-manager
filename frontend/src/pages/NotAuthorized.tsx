// pages/NotAuthorized.tsx
const NotAuthorized = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <h1 className="text-2xl font-semibold">У вас нет прав для доступа к этой странице.</h1>
        </div>
    );
};

export default NotAuthorized;
